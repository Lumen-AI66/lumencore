# Lumencore — VPS environment template
# Copy to /opt/lumencore/.env on VPS and fill in all values before deploying.
# NEVER commit the real .env file.

# --- Postgres ---
POSTGRES_USER=lumencore
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=lumencore

# --- Redis ---
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# --- Celery ---
CELERY_POOL=solo
CELERY_CONCURRENCY=2

# --- Release metadata ---
LUMENCORE_SYSTEM_PHASE=3
LUMENCORE_RELEASE_ID=phase3
LUMENCORE_RELEASE_MANIFEST_SHA256=unset

# --- Domain (used by Traefik router labels) ---
# Set to your actual domain, e.g. lumencore.yourdomain.com
LUMENCORE_DOMAIN=lumencore.yourdomain.com

# --- Optional: external API keys (add as needed) ---
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
